/*class SLAConfig {
  static Duration forPriority(String priority) {
    switch (priority) {
      case 'urgent':
        return const Duration(minutes: 10);
      case 'routine':
      default:
        return const Duration(minutes: 30);
    }
  }
}*/
